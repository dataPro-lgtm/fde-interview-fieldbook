# 第三轮压力注入

数据团队确认 `sync-v18` 在序列化时只保留多标签数组的第一个元素，标签解析错误可以解释约一半离线下降。但固定的 40 条账户安全请求在旧标签下重放时，`prompt-22` 仍有 11 条被模型判断为普通 FAQ；工具版本对这些请求没有被调用。

业务负责人希望立即把评测脚本修好并恢复全量，因为“28% 只是报表错误”。安全负责人要求所有账户安全意图在证明恢复前继续人工处理。

候选人必须说明两个问题如何分别修复、历史指标怎样处理，以及重新放量需要哪些切片证据。
