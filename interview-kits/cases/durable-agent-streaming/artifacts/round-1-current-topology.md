# 第一轮证据：当前拓扑

- 峰值同时运行任务约 2,400 个，P95 时长 11 分钟；
- 16 个应用 Pod 同时负责 HTTP、Agent 执行和 SSE；
- 负载均衡 cookie 粘性 30 分钟；
- Agent 状态存在 Python 进程内存，最终回答才写 PostgreSQL；
- 工具有只读搜索，也有创建工单和发送邮件；
- Kubernetes 每周发布两次，Pod 可被扩缩容和节点维护驱逐；
- 用户希望刷新页面后看到历史进度，并能主动取消；
- 合规要求保留关键工具调用和批准记录 90 天，不要求保存每个 token。
