# 第三轮压力注入

安全团队确认租户 B 的 `POL-9` 已在上午 10:03 撤销一组用户权限，但 14:20 仍能通过引用缓存打开旧片段。与此同时，租户 C 的对象存储列举接口因分页 token 失效只返回 40% 文件，现有同步已把另外 60% 标记软删除。

业务要求两小时内恢复搜索，同时不能重新暴露已撤销权限的内容。向量索引全量重建需要六小时，引用缓存只能按文档键主动失效。

候选人必须区分两起事件，给出立即保护、恢复顺序、影响证明和防止再次发生的发布门禁。
