# 第二轮证据：认证升级事故

- 上游 CRM 将 access token 有效期从 24 小时缩短到 60 分钟；
- 两个分支在 401 后无限重试，触发限流并延迟 9 小时；
- 一个分支刷新 token 时把新凭证写入普通错误日志；
- 文档连接器回填没有隔离实时游标，导致 14,000 条重复版本；
- 对象存储连接器把一次列表分页失败解释为缺失删除；
- 目前只有开发者能登录容器判断状态和手工改游标；
- 五个分支的共同代码比例约 55%，但错误和删除行为差异很大。
